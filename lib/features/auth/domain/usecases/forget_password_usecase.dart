/*
import 'package:dartz/dartz.dart';
import 'package:flutter/material.dart';

import '../../../../core/errors/failure.dart';
import '../repositories/auth_repositories.dart';

class ForgetPasswordUseCase {
  final AuthRepositories authRepositories;
  ForgetPasswordUseCase({required this.authRepositories});
  Future<Either<Failure, Unit>> callForgetPassword({required TextEditingController text}) {
    return authRepositories.forgetPassword(text: text);
  }

  Future<Either<Failure, Unit>> callVerify() {
    return authRepositories.verify();
  }
  Future<Either<Failure, Unit>> callVerifyChange() {
    return authRepositories.verifyChange();
  }
  Future<Either<Failure, Unit>> callChangePass() {
    return authRepositories.changePassword();
  }
}
*/
